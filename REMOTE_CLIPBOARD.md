# 远程剪贴板

入口在现有的 **工作台 → 剪贴板**，不增加悬浮按钮或占用终端工具栏。桌面可用 `Ctrl+Alt+Shift+C` 直达，`Esc` 收起。手机使用同一工作台入口。

## 远端复制到本机

当前前台、已连接终端收到 OSC 52 文本复制请求时，默认自动写入本机剪贴板。tmux 选中后释放鼠标、SSH 内的 `cb` 等操作，只要原本会发出 OSC 52，就走这条路径。成功只更新历史中的状态，不弹窗，也不打开面板。

HTTPS 优先使用浏览器 Clipboard API；远程 HTTP 尝试浏览器原生复制兼容路径。浏览器拒绝时，内容保留在历史，工作台入口显示待取标记，用户可以在那里补一次复制。后台标签、失焦页面、历史重放、首次恢复的记录不会覆盖本机剪贴板。浏览器不允许的自动写入无法由网页强行绕过。

历史可搜索内容与来源、筛选方向、固定、删除、清空，再次复制或转发。默认保留 7 天，最多 100 条 / 20 MiB，单条最多 4 MiB。设置里可改为 1/30 天、关闭自动复制或暂停历史。固定项免于按天过期，仍受数量与容量限制。

## 本机发送到远端

在“发送到远端”粘贴或编辑文本，明确选择终端 / tmux pane：

- **存到远端剪贴板**：保存为该目标的私有文本缓冲，不向终端输入字符；面板提供可直接复制的取用命令。
- **填入输入草稿**：先保留为草稿。只有点击 **确认发送到此终端**，才把文本粘贴到目标并按回车。多行文本可能由目标程序执行。

目标一经选择就不会随主终端切换。发送前重新核验会话、稳定 pane ID 和进程 PID；目标退出、替换或断开时保留草稿。发送无法确认时不自动重试。

保存的缓冲属于 deepwork-terminal 服务器上的文本文件，不是服务器桌面的系统剪贴板。嵌套 SSH 后，取用命令仍需在面板标明的服务器上执行；OSC 52 没有文件名或嵌套主机身份，界面不会猜测来源。

## 实现与维护

- 服务端在 PTY 输出流中观察 OSC 52，处理跨数据块的序列；不依赖某个可见 xterm 才记录事件。
- 事件 ID 使用会话 ID 与 daemon 输出偏移。重放单独标记；浏览器按 ID 去重。鉴权长轮询即时送达新记录，无固定一秒的轮询等待。
- 服务端传输缓存上限 100 条 / 20 MiB，仅返回最近 24 小时记录；浏览器历史按端点与认证身份隔离，使用 IndexedDB。正文不写入 localStorage；少量删除 ID 同步保存，避免清空后立即刷新恢复旧记录。
- 远端缓冲通过临时文件原子替换，文件权限 0600，目录权限 0700。可选 CLI：`dw-terminal clipboard get --data-dir <服务器数据目录> --target <目标 ID>`。
- tmux 发送用独立命名缓冲和一次命令队列，稳定寻址到 pane；不借用当前焦点，不经过可能自动重试的查询通道。

回归检查：`go test ./...`；`go test -race . -run TestRemoteClipboard`；`go test ./agentintel -run TestClipboardTmux`；`cd frontend && bun test`。浏览器验收须使用独立 muxd/tmux 和测试端口，覆盖远程 HTTP 的真实鼠标选择、原生粘贴回读、后台不覆盖、权限拒绝、跨 tab 目标固定、存储与显式发送、刷新恢复、清空后立即刷新，以及手机布局。不得把测试浏览器连接到用户正在运行的会话。
