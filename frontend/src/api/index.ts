/**
 * Terminal API surface — workbench configuration.
 *
 * Shared HTTP primitives (apiGet, apiPost, etc.) are in @ce/api/client.
 */
export { fetchWorkbenchConfig, saveWorkbenchConfig } from './workbench'
