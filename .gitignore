/dw-terminal
*.exe
*.test
*.out
bin/
*.pid
frontend/--output
frontend/node_modules/
frontend/dist/
# internal/spa/dist/ is pre-built and tracked — Go embed source, no Node.js needed to build
.env
.env.*
*.key
*.pem
credentials.*
# runtime data dir (normally ~/.dw-terminal; can appear at repo root if HOME unset).
# Holds the VAPID private key, push subscriptions, upload index — never commit.
/.dw-terminal/
# workflow_tg run scratch (ephemeral per-thread work; SSOT lives in docs/ -> pro)
/.tg/
screenshot.png
docs/
.DS_Store
.idea/
.vscode/
/docs

# goreleaser build output (root); internal/spa/dist is separate + tracked
/dist/
tmp/
